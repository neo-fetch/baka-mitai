// coding magic-square problem using OpenMP
// Compile: gcc -fopenmp magicSquare.c -o magicSquare -lm
// Run: ./magicSquare

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <omp.h>


#define N 100
#define MAX_THREADS 8

int main(int argc, char *argv[]) {
    int i, j, k, l, m, n, p, q, r, s, t, u, v, w, x, y, z;
}    